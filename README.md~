# xobin-hackathon
There are three files

1. Stackoverflow xmls is a project to parse the given XML files and display the UI.
   To run:
   a) clone or download the repository
   b) npm install
   c) node app.js

2. socketio text-chat is a project for peer to peer communication (source-https://github.com/socketio/socket.io-p2p/tree/master/examples/chat).
   To run:
   a) clone or download repository
   b) npm install
   c) browserify src/index.js -o bundle.js
   d) node server.js

3. socketio video-chat is a project for peer to peer video conference (source-https://github.com/piratefsh/webrtc-video-chat)
   To run:
   a) clone or download repository
   b) npm install
   c) node server/server.js
